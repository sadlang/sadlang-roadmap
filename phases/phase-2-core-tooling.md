# المرحلة 2 — الأدوات الأساسيّة

> **الحالة:** 🟡 جزئيّة · **المعلم:** [LSP ألفا](../milestones/M-lsp-alpha.md)

## الهدف
أدوات سطر الأوامر والمحرّر الأساسيّة التي يحتاجها أيّ مطوّر يوميًّا.

> **قرار مالك (2026-06-21):** الأدوات تبقى **in-tree** تحت `s-programming-language/tools/`
> (mono-repo)، يملكها فريق الأدوات. لا مستودع منفصل (يتجنّب كسر اعتماد `shared/`).

## النطاق
الأدوات موجودة فعلًا كمجلّدات في `tools/` (lsp, formatter, repl, profiler, pkg, analyze, ...).
الحالة أدناه = **وجود المجلّد**؛ **النضوج يحتاج تحقّقًا بالبناء/الاختبار** (GR-01).

| المكوّن | الموقع | وجود | نضوج (GR-01) |
|--------|-------|------|---------------|
| CLI (`sad-run` / `sad-build`) | الجذر | ✅ | ✅ مستقرّة |
| LSP | `tools/lsp/` | ✅ موجود | 🔍 يحتاج تحقّق |
| formatter | `tools/formatter/` | ✅ موجود | 🔍 يحتاج تحقّق |
| analyze / check | `tools/analyze/`, `tools/check/` | ✅ موجود | 🔍 يحتاج تحقّق |

## بوّابة الدخول
اجتياز بوّابة المرحلة 1 (Core مستقرّ — وإلا فالأدوات تُبنى على رمال).

## بوّابة الخروج (للانتقال إلى المرحلة 3)
- [ ] LSP ألفا: إكمال + تحويم + تشخيص تعمل في VS Code.
- [ ] formatter مستقرّ ومُختبَر على عيّنة تمثيليّة.
- [ ] CLI موثّقة وثابتة الواجهة (لا breaking changes).

## المعالم
- [LSP ألفا](../milestones/M-lsp-alpha.md)

## أوّل مهمّة فعليّة
- تدقيق نضوج `tools/lsp/` و`tools/formatter/` بالبناء/الاختبار وتثبيت الحالة (يرفع 🔍 إلى ✅/❌ بدليل).
