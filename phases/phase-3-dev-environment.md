# المرحلة 3 — بيئة التطوير

> **الحالة:** 🟡 بدأت · **المعلم:** [إضافة VS Code 1.0](../milestones/M-vscode-1.0.md)

## الهدف
تجربة تطوير كاملة: محرّر غنيّ بدعم RTL، تنقيح، REPL، قياس أداء.

> **قرار مالك (2026-06-21):** إضافات VS Code **ثلاثة منتجات متمايزة** لا نسخ مكرّرة:
> `vs_code_rtl` (محرّر RTL) · `arabic_vscode` (حزمة تعريب) · `vscode-extension` (تنفيذ سحابيّ، مرحلة 4).
>
> **إضافة (2026-06-30):** **محراب** — محرّر لغة ص **مستقلّ** (VSCodium-مرقَّع)، فئة مختلفة
> عن الإضافات الثلاث (قشرة/تطبيق لا إضافة). تخطيط فقط؛ معلم [M-mihrab-1.0](../milestones/M-mihrab-1.0.md).

## النطاق
| المكوّن | الحالة (GR-01) | الموقع |
|--------|----------------|-------|
| إضافة VS Code (RTL) | 🟡 قائمة وتتطوّر | `vs_code_rtl` (نمط Strategy، 17 اختبار + CI) |
| حزمة التعريب | 🟢 ناضجة (`.vsix`) | `arabic_vscode` (lang-pack) |
| REPL (`sad-repl` v1.0.0) | ✅ يُبنى ويُقلع؛ ⚠️ تقييم stdin المُمَرَّر لم يُلاحَظ صداه (قد يلزم TTY) | `tools/repl/` |
| profiler (`sad-profiler` v1.2.0) | ✅ **ناضج** (دليل أدناه) | `tools/profiler/` |
| debugger (المنقّح) | ⏳ لم يبدأ | — |
| محراب (محرّر مستقلّ) | ⏳ تخطيط (`مقترح`) | `sadlang-planning/projects/mihrab-editor/` (VSCodium-مرقَّع، م0–م5) |

### نتيجة تدقيق repl/profiler (2026-06-21 · بناء Debug فعليّ)
- **profiler — `sad-profiler` v1.2.0:** ✅ يُبنى؛ ✅ **يُنفّذ ملفّ ص فعليًّا** (طبع `10`) ويجمع
  أداءً حقيقيًّا (`totalTimeNs`، نداءات دوال/مدمجة، عمق المكدّس، متغيّرات/كائنات منشأة)؛
  يدعم صيغ التقرير `نص/json/html/لهب/csv`. **ناضج**.
- **REPL — `sad-repl` v1.0.0:** ✅ يُبنى ويُقلع ببانر ويخرج نظيفًا بـ`:خروج`؛ ⚠️ عند تمرير
  تعبير عبر stdin (لا-TTY) لم يظهر صدى النتيجة — يحتاج تحقّقًا تفاعليًّا (TTY) لتأكيد التقييم.
- **pkg — `sad-pkg` v2.0.0 (مرحلة 4 لكنه قائم in-tree):** ✅ يُبنى؛ ✅ CLI كامل
  (init/install/add/remove/update/list/verify + سجلّ)؛ ⚠️ 6 ملفّات اختبار في `tools/pkg/tests/`
  **غير مربوطة بالبناء**.

## بوّابة الدخول
اجتياز بوّابة المرحلة 2 (LSP ألفا — الإضافة تعتمد عليه للتشخيص/الإكمال).

## بوّابة الخروج (للانتقال إلى المرحلة 4)
- [ ] إضافة VS Code 1.0 مستقرّة (إبراز + قصاصات + أوامر + لوحة RTL + مستكشف AST).
- [ ] debugger مدمج يعمل مع المفسّر.
- [ ] REPL مستقرّ وموثّق.

## المعالم
- [إضافة VS Code 1.0](../milestones/M-vscode-1.0.md)
- [محراب 1.0 — محرّر لغة ص المستقلّ](../milestones/M-mihrab-1.0.md) (تخطيط؛ متعدّد السنوات)

## ملاحظة
المرحلة 3 **بدأت بالفعل** (إضافة RTL قائمة) لكنها تتقدّم بالتوازي مع إكمال المرحلة 2؛
البوّابة الرسميّة للمرحلة 4 تبقى مقفلة حتى تكتمل 2 و3 معًا.
