# 🛡️ الحوكمة وقواعد منع التشتّت (Anti-Chaos)

> هذه القواعد **تُفرَض** (CI + مراجعة)، لا تُقترَح. مُكيَّفة لواقع لغة ص: وكلاء متوازون عبر
> worktrees + الـRFC كوحدة عمل. تبني على PMF v1.9 ولا تنقضه.

---

## العلاقة بنظام PMF القائم

| الطبقة | الموقع | المسؤوليّة |
|--------|--------|-----------|
| **الفيدراليّة (هذا المستودع)** | `sadlang-roadmap` | مراحل + معالم عابرة للمستودعات + توجيه + منع تشتّت |
| **الحوكمة التفصيليّة** | `s-programming-language/_bmad-output/governance` (PMF) | DoD، verifiers، سبرنتات، GR-rules، نسبة العربيّة |
| **وحدة العمل/القفل** | `sadlang-rfcs` | claim الوكيل، الترقيم، حقل «الحالة» الإلزاميّ |
| **حقيقة المهمّة** | مستودع المنتج (قصّة/ستوري/كود) | التنفيذ والدليل |

**عند التعارض:** PMF يحكم التفاصيل داخل `s-programming-language`؛ هذا المستودع يحكم
ما **بين** المستودعات. لا ازدواج: المهمّة تُكتب مرّة في مصدرها، وتُفهرَس هنا.

---

## القواعد السبع

### قاعدة 1 — ممنوع فتح نطاق جديد بلا موافقة
لا مستودع/أداة/مكتبة/مرحلة جديدة إلا إذا: ✅ في خارطة الطريق · ✅ مُوافَق عليه من المالك ·
✅ له معلم · ✅ له مهمّة · ✅ له مسؤول. الطلب عبر `.github/ISSUE_TEMPLATE/new-scope.md`.

### قاعدة 2 — وكيل واحد ⇄ نطاق واحد في اللحظة (مُكيَّفة)
> الصيغة الساذجة «مطوّر واحد = مستودع واحد» تتعارض مع ~10 وكلاء متوازين. الصيغة الصحيحة:

كل **وكيل** يملك في اللحظة الواحدة **RFC واحدًا + worktree واحدًا + بطاقة مهمّة واحدة**،
ويُنهيها قبل المطالبة بأخرى. التوازي **بين الوكلاء** مسموح ومطلوب؛ التشتّت **داخل الوكيل** ممنوع.
هذا يحفظ منع التشتّت ويتوافق مع التوازي معًا.

### قاعدة 3 — ممنوع القفز بين المهام
لا تنتقل لمهمّة جديدة قبل وصول الحاليّة إلى `Done` (أو إعادتها صراحةً إلى `To Do` مع سبب موثّق).

### قاعدة 4 — ممنوع العمل على ميزات مستقبليّة
لا عمل على المرحلة 4 (نظام بيئيّ: مدير حزم، تنفيذ سحابيّ، أدوات أندرويد/iOS) ولا ما بعدها
قبل إغلاق المرحلة 1 (Core) والمرحلة 2 (الأدوات) والمرحلة 3 (بيئة التطوير) حسب بوّاباتها.
الاستثناء الوحيد: **بحث استكشافيّ موثّق (Spike)** بمعلم وحدّ زمنيّ، لا كود إنتاجيّ.

### قاعدة 5 — كل مهمّة تنتهي خلال أسبوع
المهمّة قابلة للإنهاء خلال **2–5 أيام**. تجاوزت 7 أيام؟ **تُقسَّم** إلى مهام أصغر فورًا.
(يطابق سقف PMF: 3 ستوريات/أسبوع لكل وكيل.)

### قاعدة 6 — لا «مُنجَز» بلا دليل (GR-01)
لا تُعلَّم مهمّة `Done` إلا ببناء/اختبار يثبتها (كما في جداول الأدلّة في أنظمة `_bmad-output`).
الادّعاء بلا دليل = ثغرة تُبلَّغ، لا تُخفى (راجع ذاكرة «أبلِغ عمّا يفشل لا تتجنّبه»).

### قاعدة 7 — العزل والدمج المبوَّب
العمل في worktree معزول من `dev`، الدمج عبر PR يفرض **منع التراجع** (لا انحدار في الاختبارات
الشاملة). `dev` و`sadlang` محميّان (Ruleset + توقيع). لا دفع مباشر.

---

## بوّابات المراحل (Phase Gates)

لا تُفتح مرحلة قبل اجتياز بوّابة سابقتها. تفاصيل كل بوّابة في `phases/phase-N-*.md`.

| الانتقال | البوّابة (مختصرة) |
|---------|------------------|
| 0 → 1 | المستودعات منظّمة، PMF + المعماريّة موثّقة، المنتجات الثلاثة معرّفة ✅ |
| 1 → 2 | Core يبني Release بلا تحذيرات، تنفيذ مزدوج ≥95%، أمان null مكتمل، الأنواع الموحَّدة |
| 2 → 3 | LSP ألفا (إكمال/تحويم/تشخيص)، formatter، CLI مستقرّة |
| 3 → 4 | إضافة VS Code 1.0، debugger، REPL — كلها مستقرّة |
| 4 → 5 | النظام البيئيّ الأساسيّ يعمل (مدير حزم ينشر، تنفيذ) |

---

## آليّة الفرض

- `scripts/validate_roadmap.py` يفشل CI إذا: مهمّة في مرحلة لاحقة بينما الحاليّة مفتوحة ·
  مهمّة بلا معلم/مستودع/مسؤول · معلم بلا تاريخ/DoD · مستودع بلا فريق · حالة خارج التعداد المسموح.
- مراجعة المالك إلزاميّة لتعديل المراحل أو معايير المعالم.
- المراجعة الأسبوعيّة (Weekly Sync — انظر `status/kanban.md`) تغلق المهام المعطّلة وتكشف التشتّت.
